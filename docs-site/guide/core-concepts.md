# 核心概念

## 分层模型

- `@ly-editor/core`: 文档状态、命令执行、事务回滚。
- `@ly-editor/view-snabbdom`: 视图渲染与事件桥接。
- `@ly-editor/plugin-kit`: 插件协议、工具栏元数据、i18n 解析。

## 数据流

1. 用户在视图层触发输入或点击。
2. 事件映射为命令并提交到 core。
3. core 更新快照并输出新状态。
4. view 依据快照做增量渲染。

## 扩展边界

插件通过协议扩展行为和文案，不直接侵入核心事务层。
