# 多语言（i18n）

编辑器支持通过 `locale` 与 `messages` 完成文案切换与覆盖。

## 约定

- 内置语言：`zh-CN`、`en-US`
- 业务覆盖：通过 `messages` 传入
- 建议 key：`toolbar.xxx`、`table.xxx`

## 示例（React）

```tsx
<LyEditor
  locale="en-US"
  messages={{
    'en-US': {
      'toolbar.bold': 'Strong'
    }
  }}
/>
```
