# 多语言（i18n）

编辑器组件统一支持：

- `locale`：当前语言（例如 `zh-CN`、`en-US`）
- `messages`：自定义语言包覆盖

示例（React）：

```tsx
<LyEditor
  locale="en-US"
  messages={{
    'en-US': {
      'toolbar.bold': 'Strong'
    }
  }}
/>
```
