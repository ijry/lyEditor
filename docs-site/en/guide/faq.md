# FAQ

## Why do I see title keys instead of labels?

Both active locale and fallback locale are missing translations for that key.

## Why do uploads return 403?

Usually caused by expired signatures or server time skew.
